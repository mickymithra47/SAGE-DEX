# 16 — User Liquidity Action History

## 1. Action Ledger
- Tracks deposits (`Mint`) and redemptions (`Burn`) associated with a user address.
- Returns exact block numbers, transaction hashes, and token quantities supplied/withdrawn.
