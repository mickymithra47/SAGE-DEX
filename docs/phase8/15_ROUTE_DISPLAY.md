# 15 — Route Path Visualization

## 1. Single & Multi-Hop Path Rendering
- Displays deterministic route path:
  - Single-hop: `TKNA → TKNB`
  - Two-hop: `TKNA → USDC → WETH`
- Displays individual pool addresses and fee tier (0.30% standard).
